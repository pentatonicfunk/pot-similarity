<?xml version="1.0"?>
<ruleset name="PotSimilarity CS">
    <description>PHPCS PotSimilarity</description>

    <!-- Exclude the Composer Vendor directory. -->
    <exclude-pattern>/vendor/*</exclude-pattern>

    <rule ref="PSR2">
    </rule>
</ruleset>
